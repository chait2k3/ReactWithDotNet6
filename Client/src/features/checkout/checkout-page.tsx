import { Typography } from "@mui/material";

const CheckoutPage = () => {
    return (
        <Typography variant="h3">
            Only logged user should see this page!
        </Typography>
    );
};

export default CheckoutPage;